<!doctype html>
<html lang="it">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Offerta ZeroStart Migliorshop — One Page</title>

  <!-- Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Varela+Round&display=swap" rel="stylesheet">

  <style>
    *{ box-sizing:border-box; }
    html{ scroll-behavior:smooth; }
    body{
      margin:0;
      font-family:"Varela Round", Arial, Helvetica, sans-serif;
      color:#0f172a;
      background:#fff;
    }

    :root{
      --blu:#0e2166;
      --viola:#8923ad;
      --azzurro:#19c6ff;

      --muted:#6b7280;
      --text:#0f172a;

      --arancio:#f97316;
      --arancio-bd:#fed7aa;
      --arancio-bg:#fff7ed;
      --arancio-txt:#9a3412;

      --verde:#15803d;
      --verde-bg:#ecfdf5;
      --verde-bd:#bbf7d0;

      --rosso:#ef4444;
      --rosso-bd:#fecaca;
      --rosso-bg:#fef2f2;
      --rosso-txt:#991b1b;

      --line: rgba(15,23,42,0.10);
      --shadow: 0 22px 70px rgba(15,23,42,0.10);
      --shadow2: 0 12px 30px rgba(15,23,42,0.08);

      /* più “pieno” su schermi grandi */
      --max: 1680px;
      --px: clamp(16px, 2.5vw, 28px);

      --radius: 22px;
    }

    a{ color:inherit; }

    /* container centrato come nell’esempio */
    .container{
      width: min(var(--max), 92vw);
      margin: 0 auto;
      padding: 0 var(--px);
    }

    /* ===== Header ===== */
    header{
      position: sticky;
      top: 0;
      z-index: 60;
      background: rgba(255,255,255,0.86);
      backdrop-filter: blur(14px);
      border-bottom: 1px solid rgba(15,23,42,0.07);
    }
    .header-inner{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 16px;
      padding: 12px 0;
    }
    .brand{
      display:flex;
      align-items:center;
      gap: 12px;
      min-width: 220px;
    }
    .logo{
      height: 34px;
      width:auto;
      display:block;
      filter: drop-shadow(0 8px 14px rgba(15,23,42,0.10));
    }
    .brand-txt{
      display:flex;
      flex-direction:column;
      line-height:1.1;
    }
    .brand-title{
      font-weight:900;
      letter-spacing:-0.2px;
      font-size: 14px;
      color: var(--blu);
    }
    .brand-sub{
      margin-top: 2px;
      font-size: 12px;
      color: var(--muted);
    }

    nav{
      display:flex;
      align-items:center;
      gap: 10px;
      flex-wrap:wrap;
      justify-content:flex-end;
    }
    nav a{
      text-decoration:none;
      color: var(--text);
      font-weight:900;
      font-size: 13px;
      padding: 10px 10px;
      border-radius: 12px;
      transition: background .12s ease, transform .12s ease;
      white-space:nowrap;
    }
    nav a:hover{ background: rgba(15,23,42,0.06); transform: translateY(-1px); }
    nav a.is-active{ background: rgba(14,33,102,0.07); color: var(--blu); }

    .nav-cta{
      display:inline-flex;
      align-items:center;
      gap:8px;
      padding: 10px 12px;
      border-radius: 999px;
      border: 1px solid rgba(249,115,22,0.35);
      background: linear-gradient(135deg, rgba(249,115,22,0.14), rgba(255,255,255,0.95));
      color: var(--arancio-txt);
      box-shadow: 0 10px 24px rgba(249,115,22,0.10);
    }
    .dot{
      width:8px;height:8px;border-radius:999px;
      background: linear-gradient(135deg, #f59e0b, var(--arancio));
    }

    /* ===== Sections (no “linee divisorie”) ===== */
    section{
      position: relative;
      padding: clamp(42px, 6vw, 82px) 0;
      overflow:hidden;
    }

    /* diagonali “trasversali” */
    .band-1{
      background:
        radial-gradient(circle at 10% 0%, rgba(25,198,255,0.14) 0, transparent 55%),
        radial-gradient(circle at 100% 10%, rgba(137,35,173,0.12) 0, transparent 55%),
        linear-gradient(180deg, #ffffff 0%, #fbfcff 55%, #ffffff 100%);
    }
    .band-1:before{
      content:"";
      position:absolute;
      left:-10%;
      right:-10%;
      top:-60px;
      height: 160px;
      background: linear-gradient(90deg, rgba(25,198,255,0.10), rgba(137,35,173,0.10), rgba(249,115,22,0.10));
      transform: skewY(-4deg);
      opacity: .65;
      filter: blur(1px);
      pointer-events:none;
    }

    .band-2{
      background:
        radial-gradient(circle at 0% 40%, rgba(249,115,22,0.10) 0, transparent 60%),
        radial-gradient(circle at 100% 10%, rgba(25,198,255,0.10) 0, transparent 60%),
        linear-gradient(180deg, #fbfcff 0%, #ffffff 70%);
    }
    .band-2:after{
      content:"";
      position:absolute;
      left:-12%;
      right:-12%;
      bottom:-70px;
      height: 180px;
      background: linear-gradient(90deg, rgba(14,33,102,0.08), rgba(137,35,173,0.10), rgba(25,198,255,0.08));
      transform: skewY(4deg);
      opacity: .55;
      pointer-events:none;
    }

    .band-3{
      background:
        radial-gradient(circle at 18% 0%, rgba(137,35,173,0.11) 0, transparent 55%),
        radial-gradient(circle at 92% 10%, rgba(249,115,22,0.11) 0, transparent 60%),
        linear-gradient(180deg, #ffffff 0%, #fbfcff 50%, #ffffff 100%);
    }

    /* ===== Type ===== */
    .kicker{
      display:flex;
      align-items:center;
      gap:10px;
      flex-wrap:wrap;
      margin-bottom: 16px;
    }
    .pill{
      display:inline-flex; align-items:center; gap:8px;
      border-radius:999px;
      border:1px solid #dce8ff;
      padding:8px 13px;
      font-size: 13px;
      font-weight: 900;
      color:var(--blu);
      background:#f5f7ff;
      white-space:nowrap;
      box-shadow: 0 10px 24px rgba(15,23,42,0.06);
    }
    .pill-dot{
      width:10px; height:10px; border-radius:999px;
      background:linear-gradient(135deg,var(--viola),var(--azzurro));
    }
    .pill-red{
      display:inline-flex; align-items:center; gap:8px;
      border-radius:999px;
      border:1px solid var(--rosso-bd);
      padding:8px 13px;
      font-size: 13px;
      font-weight: 900;
      color: var(--rosso-txt);
      background: var(--rosso-bg);
      white-space:nowrap;
      box-shadow: 0 10px 24px rgba(15,23,42,0.06);
    }
    .pill-red-dot{
      width:10px; height:10px; border-radius:999px;
      background: linear-gradient(135deg,#f87171,var(--rosso));
      box-shadow: 0 4px 10px rgba(239,68,68,0.18);
    }

    h1{
      margin:0;
      font-size: clamp(40px, 3.8vw, 64px);
      line-height: 1.03;
      letter-spacing: -0.9px;
      color: var(--blu);
    }
    h1 span{ color: var(--viola); }

    h2{
      margin:0;
      font-size: clamp(30px, 2.5vw, 46px);
      line-height: 1.08;
      letter-spacing: -0.6px;
      color: var(--blu);
    }
    h2 span{ color: var(--viola); }

    .lead{
      margin: 14px 0 0 0;
      color: var(--muted);
      font-size: 16px;
      line-height: 1.65;
      max-width: 70ch;
    }
    .sublead{
      margin: 12px 0 0 0;
      color: var(--muted);
      font-size: 15px;
      line-height: 1.65;
      max-width: 92ch;
    }

    /* ===== Buttons ===== */
    .cta-row{
      display:flex;
      gap: 10px;
      flex-wrap:wrap;
      align-items:center;
      margin-top: 18px;
    }
    .btn{
      display:inline-flex;
      align-items:center;
      gap:8px;
      padding: 12px 14px;
      border-radius: 999px;
      border: 1px solid rgba(15,23,42,0.14);
      background: #ffffff;
      color: var(--text);
      font-weight:900;
      font-size: 13px;
      text-decoration:none;
      box-shadow: 0 10px 24px rgba(15,23,42,0.06);
      transition: transform .12s ease, filter .12s ease, background .12s ease;
      white-space:nowrap;
    }
    .btn:hover{ transform: translateY(-1px); filter: brightness(1.01); }
    .btn.primary{
      border-color: rgba(249,115,22,0.35);
      background: linear-gradient(135deg, rgba(249,115,22,0.14), #ffffff);
      color: var(--arancio-txt);
      box-shadow: 0 14px 32px rgba(249,115,22,0.12);
    }
    .btn .btn-dot{
      width:10px;height:10px;border-radius:999px;
      background: linear-gradient(135deg,#f59e0b,var(--arancio));
    }

    /* ===== Layout blocks (meno testo per riga + più respiro) ===== */
    .hero-grid{
      display:grid;
      grid-template-columns: 1.05fr 0.95fr;
      gap: clamp(18px, 2.5vw, 34px);
      align-items:start;
      margin-top: 18px;
    }

    /* Trust chips (meno ammassate) */
    .trust-line{
      display:flex;
      gap:10px;
      flex-wrap:wrap;
      align-items:center;
      margin-top: 18px;
    }
    .trust-chip{
      display:inline-flex; align-items:center; gap:7px;
      padding:8px 12px;
      border-radius:999px;
      border:1px solid var(--verde-bd);
      background:var(--verde-bg);
      color:var(--verde);
      font-weight:900;
      font-size: 13px;
      white-space:nowrap;
    }
    .trust-dot{
      width:8px;height:8px;border-radius:999px;
      background:linear-gradient(135deg,#34d399,var(--verde));
      box-shadow: 0 4px 10px rgba(21,128,61,0.14);
    }

    /* “Highlight” (1 idea, 1 riga) */
    .highlight{
      margin-top: 20px;
      border-radius: var(--radius);
      border: 1px solid rgba(249,115,22,0.22);
      background:
        radial-gradient(circle at 0% 0%, rgba(249,115,22,0.10) 0, transparent 60%),
        linear-gradient(135deg, rgba(255,247,237,0.96), rgba(255,255,255,0.94));
      padding: 16px 18px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 12px;
      flex-wrap:wrap;
      box-shadow: var(--shadow2);
    }
    .highlight .main{
      font-weight:900;
      color: var(--arancio-txt);
      font-size: 16px;
      line-height: 1.25;
    }
    .highlight .main .hi{ color: var(--arancio); }
    .highlight .tag{
      font-weight:900;
      color: var(--blu);
      font-size: 13px;
      border: 1px solid rgba(14,33,102,0.16);
      background: rgba(239,246,255,0.85);
      border-radius: 999px;
      padding: 9px 12px;
    }

    /* Right side: “Cosa ottieni” con meno testo e più spacing */
    .side-head{
      display:flex;
      align-items:flex-start;
      justify-content:space-between;
      gap: 12px;
      margin-bottom: 10px;
    }
    .side-head .mini{
      font-weight:900;
      color: var(--muted);
      font-size: 13px;
      line-height: 1.3;
      max-width: 32ch;
    }

    .stats{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
      margin-top: 12px;
    }
    .stat{
      border: 1px solid rgba(15,23,42,0.10);
      border-radius: var(--radius);
      padding: 16px 16px;
      background: rgba(255,255,255,0.78);
      box-shadow: 0 10px 22px rgba(15,23,42,0.06);
      position:relative;
      overflow:hidden;
    }
    .stat:before{
      content:"";
      position:absolute;
      inset:-60% -60% auto auto;
      width:240px;height:240px;
      background: radial-gradient(circle at 30% 30%, rgba(25,198,255,0.14), transparent 60%);
      transform: rotate(18deg);
      pointer-events:none;
    }
    .stat .k{
      font-size: 12px;
      color: var(--muted);
      font-weight: 900;
      position:relative;
    }
    .stat .v{
      margin-top: 10px;
      font-size: 28px;
      font-weight: 900;
      color: #111827;
      letter-spacing:-0.2px;
      position:relative;
    }
    .stat .chip{
      margin-top: 12px;
      display:inline-flex; align-items:center; gap:8px;
      padding:8px 12px;
      border-radius:999px;
      border:1px solid var(--verde-bd);
      background:var(--verde-bg);
      color:var(--verde);
      font-weight:900;
      font-size: 12px;
      white-space:nowrap;
      position:relative;
    }
    .stat .chip .d{
      width:10px;height:10px;border-radius:999px;
      background:linear-gradient(135deg,#34d399,var(--verde));
    }

    /* ===== “Come funziona” (3 step, testo corto) ===== */
    .steps{
      margin-top: 22px;
      display:grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 16px;
    }
    .step{
      border-radius: var(--radius);
      padding: 18px 18px;
      background:
        radial-gradient(circle at 0% 0%, rgba(137,35,173,0.09) 0, transparent 60%),
        rgba(255,255,255,0.78);
      border: 1px solid rgba(15,23,42,0.10);
      box-shadow: var(--shadow2);
    }
    .step .n{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      width:34px;height:34px;
      border-radius: 14px;
      background: rgba(239,246,255,0.90);
      border: 1px solid rgba(14,33,102,0.16);
      color: var(--blu);
      font-weight:900;
    }
    .step h3{
      margin: 12px 0 6px 0;
      font-size: 16px;
      color:#111827;
      letter-spacing:-0.2px;
    }
    .step p{
      margin:0;
      color: var(--muted);
      font-size: 13.5px;
      line-height: 1.55;
      max-width: 44ch;
    }

    /* ===== Plans (snelli + tabella pulita) ===== */
    .plans-head{
      display:flex;
      align-items:flex-end;
      justify-content:space-between;
      gap: 14px;
      flex-wrap:wrap;
      margin-bottom: 18px;
    }
    .badge-orange{
      display:inline-flex; align-items:center; gap:8px;
      padding:9px 13px;
      border-radius:999px;
      border:1px solid var(--arancio-bd);
      background: var(--arancio-bg);
      color: var(--arancio-txt);
      font-weight:900;
      font-size: 13px;
      white-space:nowrap;
      box-shadow: 0 10px 24px rgba(15,23,42,0.06);
    }
    .badge-dot{
      width:10px;height:10px;border-radius:999px;
      background: linear-gradient(135deg,#f59e0b,var(--arancio));
      box-shadow: 0 4px 10px rgba(249,115,22,0.16);
    }

    .plans-grid{
      display:grid;
      grid-template-columns: 0.95fr 1.05fr;
      gap: 18px;
      align-items:start;
      margin-top: 10px;
    }

    .callout{
      border-radius: var(--radius);
      padding: 18px 18px;
      background:
        radial-gradient(circle at 100% 0%, rgba(25,198,255,0.12) 0, transparent 60%),
        linear-gradient(135deg, rgba(255,255,255,0.92), rgba(239,246,255,0.72));
      border: 1px solid rgba(14,33,102,0.12);
      box-shadow: var(--shadow2);
    }
    .callout .t{
      font-weight:900;
      color: var(--blu);
      font-size: 16px;
      margin:0;
    }
    .callout .t span{ color: var(--viola); }
    .callout .p{
      margin:10px 0 0 0;
      color: var(--muted);
      font-size: 13.5px;
      line-height: 1.55;
      max-width: 70ch;
    }
    .note{
      margin-top: 14px;
      border-radius: var(--radius);
      padding: 14px 16px;
      background: rgba(248,250,252,0.85);
      border: 1px dashed rgba(14,33,102,0.20);
      color: #374151;
      font-size: 13px;
      line-height: 1.55;
    }

    table{ border-collapse:collapse; width:100%; }
    .cs-table{
      width:100%;
      border-radius: var(--radius);
      overflow:hidden;
      border:1px solid rgba(15,23,42,0.10);
      background: rgba(255,255,255,0.86);
      box-shadow: var(--shadow);
    }
    .cs-table th, .cs-table td{
      border:1px solid rgba(15,23,42,0.08);
      padding: 14px 12px;
      font-size: 13px;
      vertical-align: middle;
    }
    .cs-table th{
      background:#f5f7ff;
      color:#111827;
      text-align:left;
    }
    .cs-col-plan{ text-align:center; }
    .cs-annual{ background:linear-gradient(135deg,#e0edff,#f5f7ff); }
    .cs-triennial{ background:linear-gradient(135deg,#f4e8ff,#fdf2ff); }
    .cs-plan-title{ font-size: 14px; font-weight:900; line-height:1.1; }
    .cs-badge{ display:block; font-size:12.5px; color:var(--muted); margin-top:4px; font-weight:800; }
    .cs-name{ font-weight:900; font-size: 13.5px; margin-bottom: 3px; }
    .cs-desc{ font-size: 12.5px; color: var(--muted); }
    .cs-price{ font-weight:900; font-size: 14.5px; display:inline-block; }
    .cs-saving{
      margin-top: 8px;
      font-size: 12.5px;
      color: var(--verde);
      font-weight: 900;
      display:inline-flex;
      align-items:center;
      gap:8px;
      justify-content:center;
    }
    .save-pill{
      display:inline-block;
      padding:5px 11px;
      border-radius:999px;
      background:#ecfdf5;
      border:1px solid #bbf7d0;
    }

    /* ===== Packages (meno “ammassato”: lista in accordion) ===== */
    .pkg-grid{
      margin-top: 22px;
      display:grid;
      grid-template-columns: 1fr 1.05fr 1fr;
      gap: 16px;
      align-items:stretch;
    }
    .pkg{
      border-radius: 26px;
      padding: 18px 18px;
      background: rgba(255,255,255,0.82);
      border: 1px solid rgba(15,23,42,0.10);
      box-shadow: var(--shadow2);
      position:relative;
      overflow:hidden;
      display:flex;
      flex-direction:column;
      gap: 12px;
    }
    .pkg:before{
      content:"";
      position:absolute;
      inset:-70% -70% auto auto;
      width: 280px; height: 280px;
      background: radial-gradient(circle at 30% 30%, rgba(137,35,173,0.12), transparent 60%);
      transform: rotate(18deg);
      pointer-events:none;
    }
    .pkg.featured{
      border: 2px solid rgba(137,35,173,0.22);
      background:
        radial-gradient(circle at 0% 0%, rgba(25,198,255,0.10) 0, transparent 60%),
        radial-gradient(circle at 100% 0%, rgba(137,35,173,0.10) 0, transparent 60%),
        rgba(255,255,255,0.88);
      box-shadow: 0 30px 100px rgba(137,35,173,0.10);
      transform: translateY(-6px);
    }

    .pkg-top{
      display:flex;
      justify-content:space-between;
      align-items:flex-start;
      gap: 10px;
      position:relative;
    }
    .pkg h3{
      margin:0;
      font-size: 18px;
      font-weight:900;
      color:#111827;
    }
    .pkg .desc{
      margin-top: 6px;
      color: var(--muted);
      font-size: 13.5px;
      line-height: 1.55;
      max-width: 44ch;
      position:relative;
    }

    .chip-pill{
      display:inline-flex; align-items:center; gap:8px;
      border-radius:999px;
      padding:8px 12px;
      border:1px solid rgba(14,33,102,0.16);
      background: rgba(239,246,255,0.86);
      color: var(--blu);
      font-weight:900;
      font-size: 12.5px;
      white-space:nowrap;
      position:relative;
    }
    .chip-pill .d{ width:10px;height:10px;border-radius:999px; background: linear-gradient(135deg,var(--azzurro),var(--viola)); }

    .chip-orange{
      display:inline-flex; align-items:center; gap:8px;
      border-radius:999px;
      padding:8px 12px;
      border:1px solid var(--arancio-bd);
      background: var(--arancio-bg);
      color: var(--arancio-txt);
      font-weight:900;
      font-size: 12.5px;
      white-space:nowrap;
      position:relative;
    }
    .chip-orange .d{ width:10px;height:10px;border-radius:999px; background: linear-gradient(135deg,#f59e0b,var(--arancio)); }

    .price{
      border-radius: 18px;
      padding: 12px 12px;
      background: rgba(255,247,237,0.72);
      border: 1px solid rgba(249,115,22,0.20);
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 10px;
      flex-wrap:wrap;
      position:relative;
    }
    .price .p{
      font-weight: 900;
      color: var(--arancio-txt);
      font-size: 15px;
    }
    .price .tag{
      font-weight: 900;
      color: var(--blu);
      font-size: 12px;
      border: 1px solid rgba(14,33,102,0.16);
      background: rgba(239,246,255,0.86);
      border-radius: 999px;
      padding: 8px 10px;
    }

    .mini-kpis{
      display:flex;
      gap: 10px;
      position:relative;
      flex-wrap:wrap;
    }
    .mini{
      flex:1;
      min-width: 220px;
      border-radius: 18px;
      padding: 12px 12px;
      background: rgba(255,255,255,0.78);
      border: 1px solid rgba(15,23,42,0.10);
    }
    .mini .k{ font-size: 12px; color: var(--muted); font-weight:900; }
    .mini .v{ margin-top: 8px; font-size: 20px; font-weight:900; color:#111827; }
    .mini .chip{
      margin-top: 10px;
      display:inline-flex; align-items:center; gap:8px;
      padding:8px 12px;
      border-radius:999px;
      border:1px solid var(--verde-bd);
      background:var(--verde-bg);
      color:var(--verde);
      font-weight:900;
      font-size: 12px;
      white-space:nowrap;
    }
    .mini .chip .d{ width:10px;height:10px;border-radius:999px; background:linear-gradient(135deg,#34d399,var(--verde)); }

    details{
      position:relative;
      border-radius: 18px;
      background: rgba(248,250,252,0.72);
      border: 1px solid rgba(15,23,42,0.10);
      padding: 10px 12px;
    }
    summary{
      cursor:pointer;
      list-style:none;
      font-weight:900;
      color:#111827;
      font-size: 13px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:10px;
      user-select:none;
    }
    summary::-webkit-details-marker{ display:none; }
    .chev{
      width: 10px; height: 10px;
      border-right: 2px solid rgba(15,23,42,0.45);
      border-bottom: 2px solid rgba(15,23,42,0.45);
      transform: rotate(45deg);
      transition: transform .18s ease;
    }
    details[open] .chev{ transform: rotate(225deg); }

    ul.inc{
      margin: 10px 0 0 0;
      padding-left: 18px;
      color: var(--muted);
      font-size: 13px;
      line-height: 1.55;
    }

    .tipbar{
      margin-top: 18px;
      border-radius: var(--radius);
      border: 1px solid rgba(249,115,22,0.18);
      background: linear-gradient(135deg, rgba(255,247,237,0.92), rgba(255,255,255,0.92));
      padding: 16px 18px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 12px;
      flex-wrap:wrap;
      box-shadow: var(--shadow2);
    }
    .tipbar .t{
      font-weight: 900;
      color: var(--arancio-txt);
      font-size: 15px;
      line-height: 1.25;
    }
    .tipbar .t .hi{ color: var(--arancio); }
    .tipbar .s{
      font-weight: 900;
      color: var(--blu);
      font-size: 13px;
      border: 1px solid rgba(14,33,102,0.16);
      background: rgba(239,246,255,0.88);
      border-radius: 999px;
      padding: 9px 12px;
    }

    /* ===== Reveal ===== */
    .reveal{
      opacity:0;
      transform: translateY(14px);
      transition: opacity .65s ease, transform .65s ease;
      will-change: opacity, transform;
    }
    .reveal.is-in{ opacity:1; transform: translateY(0px); }

    /* ===== Footer ===== */
    footer{
      padding: 36px 0 52px;
      color: var(--muted);
      font-size: 12px;
      background:
        radial-gradient(circle at 20% 0%, rgba(25,198,255,0.10) 0, transparent 60%),
        radial-gradient(circle at 90% 20%, rgba(137,35,173,0.08) 0, transparent 60%),
        #ffffff;
    }
    .foot{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 12px;
      flex-wrap:wrap;
    }
    .backtop{
      text-decoration:none;
      font-weight:900;
      color: var(--blu);
      padding: 10px 12px;
      border-radius: 12px;
      transition: background .12s ease, transform .12s ease;
    }
    .backtop:hover{ background: rgba(14,33,102,0.06); transform: translateY(-1px); }

    /* ===== Responsive ===== */
    @media (max-width: 1100px){
      .hero-grid{ grid-template-columns: 1fr; }
      .steps{ grid-template-columns: 1fr; }
      .plans-grid{ grid-template-columns: 1fr; }
      .pkg-grid{ grid-template-columns: 1fr; }
      .pkg.featured{ transform:none; }
    }
    @media (max-width: 620px){
      .brand-txt{ display:none; }
      nav a{ padding: 10px 8px; }
      .stats{ grid-template-columns: 1fr; }
      .mini{ min-width: 0; }
    }
  </style>
</head>

<body>

  <!-- ===== MENU ===== -->
  <header>
    <div class="container">
      <div class="header-inner">
        <div class="brand">
          <img class="logo" src="http://img.mailinblue.com/9212108/images/68cc492abd5f0_1758218538.png" alt="Logo Migliorshop">
          <div class="brand-txt">
            <div class="brand-title">Offerta ZeroStart Migliorshop</div>
            <div class="brand-sub">One page (scroll)</div>
          </div>
        </div>

        <nav aria-label="Menu">
          <a href="#offerta">Offerta</a>
          <a href="#come-funziona">Come funziona</a>
          <a href="#piani">Piani</a>
          <a href="#pacchetti">Pacchetti</a>
          <a class="nav-cta" href="https://calendar.app.google/uFHDD9vTSKsrS3dp7" target="_blank" rel="noopener noreferrer">
            <span class="dot"></span> Prenota call
          </a>
        </nav>
      </div>
    </div>
  </header>

  <main>

    <!-- ===== HERO / OFFERTA ===== -->
    <section id="offerta" class="band-1">
      <div class="container">

        <div class="kicker reveal">
          <div class="pill"><span class="pill-dot"></span>Offerta ZeroStart Migliorshop</div>
          <div class="pill-red"><span class="pill-red-dot"></span>Valida fino all’ 11/02/2026</div>
        </div>

        <div class="hero-grid">
          <!-- left -->
          <div class="reveal">
            <h1>Lancia il tuo e-commerce <span>senza costi</span> di avvio.</h1>
            <p class="lead">
              Sito, piattaforma e grossista inclusi. Tu investi solo in Content &amp; Strategy per partire forte e scalare.
            </p>

            <div class="trust-line">
              <span class="trust-chip"><span class="trust-dot"></span>0% commissioni</span>
              <span class="trust-chip"><span class="trust-dot"></span>Sito gratis</span>
              <span class="trust-chip"><span class="trust-dot"></span>Piattaforma inclusa</span>
              <span class="trust-chip"><span class="trust-dot"></span>Grossista incluso</span>
            </div>

            <div class="cta-row">
              <a class="btn primary" href="https://calendar.app.google/uFHDD9vTSKsrS3dp7" target="_blank" rel="noopener noreferrer">
                <span class="btn-dot"></span> Blocca offerta
              </a>
              <a class="btn" href="#piani">Vedi piani</a>
              <a class="btn" href="#pacchetti">Confronta pacchetti</a>
            </div>

            <div class="highlight">
              <div class="main">Avvia gratis · <span class="hi">Risparmi oltre 12.500€</span></div>
              <div class="tag">Investi solo in ciò che rende</div>
            </div>
          </div>

          <!-- right -->
          <div class="reveal">
            <div class="side-head">
              <div>
                <h2 style="margin:0;">Cosa ottieni <span>subito</span></h2>
                <p class="sublead" style="margin-top:10px;">Pronto a vendere in 30–40 giorni lavorativi.</p>
              </div>
              <div class="mini">4 punti chiave, chiari e veloci.</div>
            </div>

            <div class="stats">
              <div class="stat">
                <div class="k">Setup iniziale</div>
                <div class="v">€ 0</div>
                <div class="chip"><span class="d"></span>ZeroStart attivo</div>
              </div>
              <div class="stat">
                <div class="k">Commissioni</div>
                <div class="v">0%</div>
                <div class="chip"><span class="d"></span>Sul venduto</div>
              </div>
              <div class="stat">
                <div class="k">Piattaforma</div>
                <div class="v">Inclusa</div>
                <div class="chip"><span class="d"></span>Gestione facile</div>
              </div>
              <div class="stat">
                <div class="k">Grossista</div>
                <div class="v">Incluso</div>
                <div class="chip"><span class="d"></span>Catalogo pronto</div>
              </div>
            </div>
          </div>
        </div>

      </div>
    </section>

    <!-- ===== COME FUNZIONA ===== -->
    <section id="come-funziona" class="band-2">
      <div class="container">
        <div class="reveal">
          <h2>Un percorso <span>semplice</span></h2>
          <p class="sublead">
            Ti guidiamo step by step: meno complessità, più chiarezza. (Ogni blocco: un’idea, poche righe.)
          </p>
        </div>

        <div class="steps">
          <div class="step reveal">
            <div class="n">1</div>
            <h3>Scelta del piano</h3>
            <p>Selezioni Basic, Standard o Premium. Poi partiamo con la roadmap.</p>
          </div>

          <div class="step reveal">
            <div class="n">2</div>
            <h3>Setup &amp; go-live</h3>
            <p>Attiviamo sito, piattaforma e grossista. Obiettivo: essere online e pronti.</p>
          </div>

          <div class="step reveal">
            <div class="n">3</div>
            <h3>Ottimizzazione &amp; crescita</h3>
            <p>Contenuti, strategia e test continui. Misuriamo e miglioriamo mese dopo mese.</p>
          </div>
        </div>

        <div class="cta-row reveal" style="margin-top:22px;">
          <a class="btn primary" href="https://calendar.app.google/uFHDD9vTSKsrS3dp7" target="_blank" rel="noopener noreferrer">
            <span class="btn-dot"></span> Prenota call
          </a>
          <a class="btn" href="#piani">Vai ai piani</a>
        </div>
      </div>
    </section>

    <!-- ===== PIANI ===== -->
    <section id="piani" class="band-3">
      <div class="container">

        <div class="plans-head reveal">
          <div>
            <h2>Piani Content &amp; Strategy</h2>
            <p class="sublead">Annuale = flessibile. Triennale = più conveniente, con risparmi annui certi.</p>
          </div>
          <div class="badge-orange"><span class="badge-dot"></span>Risparmi fino a 3.000€ / anno</div>
        </div>

        <div class="plans-grid">
          <div class="reveal">
            <div class="callout">
              <p class="t">Focus: <span>Triennale</span></p>
              <p class="p">Se vuoi massimizzare il risparmio, la durata premia: stessi obiettivi, più convenienza.</p>
              <div class="cta-row" style="margin-top:14px;">
                <a class="btn primary" href="https://calendar.app.google/uFHDD9vTSKsrS3dp7" target="_blank" rel="noopener noreferrer">
                  <span class="btn-dot"></span> Fissa un appuntamento
                </a>
                <a class="btn" href="#pacchetti">Vedi pacchetti</a>
              </div>
            </div>

            <div class="note">
              <strong style="color:var(--blu);">Note</strong><br>
              Prezzi IVA esclusa. Offerta valida previa selezione di un piano (Basic/Standard/Premium).
              Campagne pubblicitarie e Farmadati (200€/anno) non inclusi.
            </div>
          </div>

          <div class="reveal">
            <table class="cs-table" aria-label="Tabella piani Content & Strategy">
              <thead>
                <tr>
                  <th>Pacchetto</th>
                  <th class="cs-col-plan cs-annual">
                    <span class="cs-plan-title" style="color:var(--blu);">Annuale</span>
                    <span class="cs-badge">(12 mesi)</span>
                  </th>
                  <th class="cs-col-plan cs-triennial">
                    <span class="cs-plan-title" style="color:var(--viola);">Triennale</span>
                    <span class="cs-badge">(36 mesi)</span>
                  </th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td>
                    <div class="cs-name">Content &amp; Strategy Basic</div>
                    <div class="cs-desc">Per chi si affaccia all’e-commerce</div>
                  </td>
                  <td class="cs-col-plan cs-annual"><span class="cs-price">399€ / mese</span></td>
                  <td class="cs-col-plan cs-triennial">
                    <div class="cs-price">299€ / mese</div>
                    <div class="cs-saving"><span class="save-pill">Risparmi 1.200€ / anno</span></div>
                  </td>
                </tr>

                <tr>
                  <td>
                    <div class="cs-name">Content &amp; Strategy Standard</div>
                    <div class="cs-desc">Per chi punta in alto</div>
                  </td>
                  <td class="cs-col-plan cs-annual"><span class="cs-price">799€ / mese</span></td>
                  <td class="cs-col-plan cs-triennial">
                    <div class="cs-price">599€ / mese</div>
                    <div class="cs-saving"><span class="save-pill">Risparmi 2.400€ / anno</span></div>
                  </td>
                </tr>

                <tr>
                  <td>
                    <div class="cs-name">Content &amp; Strategy Premium</div>
                    <div class="cs-desc">Per chi vuole il meglio</div>
                  </td>
                  <td class="cs-col-plan cs-annual"><span class="cs-price">1.049€ / mese</span></td>
                  <td class="cs-col-plan cs-triennial">
                    <div class="cs-price">799€ / mese</div>
                    <div class="cs-saving"><span class="save-pill">Risparmi 3.000€ / anno</span></div>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>

      </div>
    </section>

    <!-- ===== PACCHETTI ===== -->
    <section id="pacchetti" class="band-2">
      <div class="container">

        <div class="reveal">
          <h2>I 3 pacchetti: <span>Basic · Standard · Premium</span></h2>
          <p class="sublead">
            Tre livelli, stesso obiettivo: partire bene, ottimizzare in modo continuo, scalare con una strategia chiara.
          </p>
        </div>

        <div class="pkg-grid">

          <!-- BASIC -->
          <article class="pkg reveal">
            <div class="pkg-top">
              <div>
                <h3>Basic</h3>
                <div class="desc">Per partire senza stress e validare.</div>
              </div>
              <div class="chip-pill"><span class="d"></span> Starter</div>
            </div>

            <div class="price">
              <div class="p">399€ / mese</div>
              <div class="tag">Setup + base</div>
            </div>

            <div class="mini-kpis">
              <div class="mini">
                <div class="k">Contenuti/mese</div>
                <div class="v">8</div>
                <div class="chip"><span class="d"></span>SEO base</div>
              </div>
              <div class="mini">
                <div class="k">Call strategia</div>
                <div class="v">1</div>
                <div class="chip"><span class="d"></span>60 min</div>
              </div>
            </div>

            <details>
              <summary>Include <span class="chev"></span></summary>
              <ul class="inc">
                <li>Setup tracking (GA4 + eventi principali)</li>
                <li>Ottimizzazione home + 5 schede prodotto</li>
                <li>8 contenuti/mese (blog o landing)</li>
                <li>1 report mensile con priorità</li>
                <li>Supporto via email (48h)</li>
              </ul>
            </details>
          </article>

          <!-- STANDARD (featured) -->
          <article class="pkg featured reveal">
            <div class="pkg-top">
              <div>
                <h3>Standard</h3>
                <div class="desc">Per crescere in modo costante e misurabile.</div>
              </div>
              <div class="pill" style="background:#fdf2ff;border-color:#f5d0fe;color:var(--viola); box-shadow:none;">
                <span class="pill-dot"></span> Best choice
              </div>
            </div>

            <div class="price">
              <div class="p">799€ / mese</div>
              <div class="tag">Growth</div>
            </div>

            <div class="mini-kpis">
              <div class="mini">
                <div class="k">Contenuti/mese</div>
                <div class="v">16</div>
                <div class="chip"><span class="d"></span>SEO + CRO</div>
              </div>
              <div class="mini">
                <div class="k">Call strategia</div>
                <div class="v">2</div>
                <div class="chip"><span class="d"></span>Roadmap</div>
              </div>
            </div>

            <details>
              <summary>Include <span class="chev"></span></summary>
              <ul class="inc">
                <li>Setup tracking completo + dashboard KPI</li>
                <li>Ottimizzazione categorie + 15 schede prodotto</li>
                <li>16 contenuti/mese (landing + blog + microcopy)</li>
                <li>2 test CRO/mese (checkout, schede, CTA)</li>
                <li>Report + call: priorità settimanali</li>
                <li>Supporto WhatsApp (24h)</li>
              </ul>
            </details>

            <div class="cta-row" style="margin-top:4px;">
              <a class="btn primary" href="https://calendar.app.google/uFHDD9vTSKsrS3dp7" target="_blank" rel="noopener noreferrer" style="box-shadow:none;">
                <span class="btn-dot"></span> Consigliato
              </a>
            </div>
          </article>

          <!-- PREMIUM -->
          <article class="pkg reveal">
            <div class="pkg-top">
              <div>
                <h3>Premium</h3>
                <div class="desc">Per chi vuole il massimo (scaling + iterazioni).</div>
              </div>
              <div class="chip-orange"><span class="d"></span> Elite</div>
            </div>

            <div class="price">
              <div class="p">1.049€ / mese</div>
              <div class="tag">Scale</div>
            </div>

            <div class="mini-kpis">
              <div class="mini">
                <div class="k">Contenuti/mese</div>
                <div class="v">28</div>
                <div class="chip"><span class="d"></span>SEO avanzata</div>
              </div>
              <div class="mini">
                <div class="k">Call strategia</div>
                <div class="v">4</div>
                <div class="chip"><span class="d"></span>Weekly</div>
              </div>
            </div>

            <details>
              <summary>Include <span class="chev"></span></summary>
              <ul class="inc">
                <li>Audit completo + piano 90 giorni</li>
                <li>Ottimizzazione full-catalog (priorità top seller)</li>
                <li>28 contenuti/mese (landing, blog, email, ads copy)</li>
                <li>4 test CRO/mese + heatmap review</li>
                <li>Supporto prioritario (entro 6h)</li>
                <li>Workshop mensile (team + roadmap)</li>
              </ul>
            </details>
          </article>

        </div>

        <div class="tipbar reveal">
          <div class="t">Tip: con <span class="hi">contratto triennale</span> massimizzi il risparmio</div>
          <div class="s">Scegli “Standard” per l’equilibrio migliore</div>
        </div>

        <div class="cta-row reveal" style="margin-top:18px;">
          <a class="btn primary" href="https://calendar.app.google/uFHDD9vTSKsrS3dp7" target="_blank" rel="noopener noreferrer">
            <span class="btn-dot"></span> Prenota call
          </a>
          <a class="btn" href="#offerta">Torna all’offerta</a>
        </div>

      </div>
    </section>

  </main>

  <footer>
    <div class="container">
      <div class="foot">
        <div>
          Prezzi IVA esclusa. Offerta valida previa selezione di un piano (Basic/Standard/Premium).
          Campagne pubblicitarie e Farmadati (200€/anno) non inclusi.
        </div>
        <a class="backtop" href="#offerta">Torna su ↑</a>
      </div>
    </div>
  </footer>

  <script>
    // Soft reveal on scroll
    (function(){
      const els = Array.from(document.querySelectorAll('.reveal'));
      const io = new IntersectionObserver((entries) => {
        entries.forEach((e) => {
          if(e.isIntersecting){
            e.target.classList.add('is-in');
            io.unobserve(e.target);
          }
        });
      }, { threshold: 0.12 });
      els.forEach(el => io.observe(el));
    })();

    // Active menu highlight
    (function(){
      const links = Array.from(document.querySelectorAll('nav a[href^="#"]')).filter(a => !a.classList.contains('nav-cta'));
      const targets = links.map(a => document.querySelector(a.getAttribute('href'))).filter(Boolean);

      const setActive = (id) => {
        links.forEach(a => a.classList.toggle('is-active', a.getAttribute('href') === '#' + id));
      };

      const io = new IntersectionObserver((entries) => {
        const vis = entries
          .filter(e => e.isIntersecting)
          .sort((a,b) => b.intersectionRatio - a.intersectionRatio)[0];
        if(vis && vis.target && vis.target.id) setActive(vis.target.id);
      }, { threshold: [0.22, 0.35, 0.55] });

      targets.forEach(t => io.observe(t));
      setActive('offerta');
    })();
  </script>

</body>
</html>
